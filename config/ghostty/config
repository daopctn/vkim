# This is the configuration file for Ghostty.
#
# This template file has been automatically created at the following
# path since Ghostty couldn't find any existing config files on your system:
#
#   /home/daopctn/.config/ghostty/config
#
# The template does not set any default options, since Ghostty ships
# with sensible defaults for all options. Users should only need to set
# options that they want to change from the default.
#
# Run `ghostty +show-config --default --docs` to view a list of
# all available config options and their default values.
#
# Additionally, each config option is also explained in detail
# on Ghostty's website, at https://ghostty.org/docs/config.
#
# Ghostty can reload the configuration while running by using the menu
# options or the bound key (default: Command + Shift + comma on macOS and
# Control + Shift + comma on other platforms). Not all config options can be
# reloaded while running; some only apply to new windows and others may require
# a full restart to take effect.

# Theme
theme = manga-mono

# Font
font-family = CaskaydiaCove Nerd Font
font-size = 11

# Background image (from Terminator config)
background-image = /home/daopctn/vkim/config/ghostty/terminal-background.jpeg
background-image-fit = stretch
background-image-opacity = 0.01

# Start maximized (full screen windowed)
fullscreen = false

# Window padding/margins (from Kitty config: window_margin_width 5)
window-padding-x = 5
window-padding-y = 5

# Cursor
cursor-style-blink = true
cursor-color = #4a9eff
cursor-opacity = 0.7
custom-shader = ~/.config/ghostty/shaders/cursor_smear.glsl
custom-shader-animation = true

# -------------------------------------------
# Terminator-style keybindings
# -------------------------------------------

# Split terminals
keybind = ctrl+shift+o=new_split:down
keybind = ctrl+shift+e=new_split:right

# Navigate between splits (Ctrl+Shift+Arrow)
keybind = ctrl+shift+up=goto_split:top
keybind = ctrl+shift+down=goto_split:bottom
keybind = ctrl+shift+left=goto_split:left
keybind = ctrl+shift+right=goto_split:right

# Resize splits (Alt+Arrow)
keybind = alt+up=resize_split:up,10
keybind = alt+down=resize_split:down,10
keybind = alt+left=resize_split:left,10
keybind = alt+right=resize_split:right,10

# Toggle zoom/maximize pane (Ctrl+Shift+X)
keybind = ctrl+shift+x=toggle_split_zoom

# Close terminal pane (Ctrl+Shift+W)
keybind = ctrl+shift+w=close_surface

# Tabs
keybind = ctrl+shift+t=new_tab
keybind = ctrl+page_up=previous_tab
keybind = ctrl+page_down=next_tab

# New window
keybind = ctrl+shift+n=new_window

# Fullscreen
keybind = ctrl+shift+f11=toggle_fullscreen

# -------------------------------------------
# Vim-style scrollback
# -------------------------------------------

# j/k — scroll by line
keybind = ctrl+shift+j=scroll_page_lines:3
keybind = ctrl+shift+k=scroll_page_lines:-3

# d/u — half page (Ctrl+D / Ctrl+U feel)
keybind = ctrl+shift+d=scroll_page_fractional:0.5
keybind = ctrl+shift+u=scroll_page_fractional:-0.5

# f/b — full page
keybind = ctrl+shift+f=scroll_page_down
keybind = ctrl+shift+b=scroll_page_up

# G / gg — top / bottom
keybind = ctrl+shift+end=scroll_to_bottom
keybind = ctrl+shift+home=scroll_to_top

# Quit
keybind = ctrl+shift+q=quit
